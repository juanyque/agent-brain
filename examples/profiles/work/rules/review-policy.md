# Review policy

Use the repository's own review instructions when they are more specific than this environment
default. Require explicit confirmation before any external publication or status transition.
